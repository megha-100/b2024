package javabasics;

public class Java_basics {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

		/**primitive data type -byte,short,int,long,float,double,char,boolean
		all keywords are start with small letters 
		all keywords are reserved words but not all reserved words are keywords 
		Non primitive data type-class,arrays,enum,String,collections,interface
		byte-8bits->-128 to 127
		short-2bytes-16bits->-2^16 to 2^16-1
		int-4bytes-32bits->-2^32 to 2^32-1
		long-8bytes-8*8=64bits->-2^64 to 2^64-1->we should explicitly mention L at last
		float-4bytes->we should explicitly mention f at last after . it can take up to 6-7 decimal digits 
		before . it can take up to 39 digits
		double-8bytes
		after . it can take up to 15 decimal digits 
		before . it can take up to 309 digits
		char-2bytes-ex-'a','$','1'(0-9) ie 't'+10 = 126 allowed
		'65','-1','$$' not allowed
		unicode values and ascii values
		boolean-1bit-false or true**/
	}

}
